name: hostinger
on:
  push:
    branches:
      - main

jobs:
  FTP-Deploy-Action:
    name: FTP Deploy to Hostinger
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2.1.0
        with:
          fetch-depth: 2

      - name: FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          server: 31.97.132.42
          username: backend_ftp
          password: "84Qu9AyvlU8e1Vt7Am4T"
          protocol: ftp
          port: 21
          server-dir: admin.gogobarter.com/
          dry-run: false
